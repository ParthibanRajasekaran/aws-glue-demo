[pytest]
pythonpath = .
markers =
    unit: marks tests as unit tests (no AWS calls)
    integration: marks tests as integration tests (requires live AWS)
    e2e: marks tests as end-to-end tests (requires deployed stack)
